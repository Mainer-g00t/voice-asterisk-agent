Stage all changes in the repo, write a concise conventional commit message that summarizes what changed (feat/fix/chore prefix as appropriate), commit, and push to origin main.

Show the git diff summary before committing so the user can see what's being pushed.
