[general]
enabled = yes
port = 5038
bindaddr = 0.0.0.0

; Internal-only AMI user for the config-api outbound originate calls.
; Secret is set via AMI_SECRET env var (substituted by docker-entrypoint.sh).
[voiceagent]
secret = ${AMI_SECRET}
read = all
write = originate,call,system
