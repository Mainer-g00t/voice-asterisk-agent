[from-softphone]
exten => _X.,1,NoOp(Incoming call to ${EXTEN} — routing to AI agent)
 same => n,Answer()
 same => n,Set(CALL_UUID=${SHELL(head -c 36 /proc/sys/kernel/random/uuid)})
 same => n,AudioSocket(${CALL_UUID},agent:9099)
 same => n,Hangup()
